<?php

// todo
class Hoge{
    public function hige($h, $n)
    {
        // todo
        // json_encode($json);
        foreach ($h as $r)
        {
            return $r;
        }
        print "aa";

        var_dump($h);

        $BAD_NAME = "";

        $num = (int) '100';
        if (1 === 1) $h + 100;
        return $h;
    }
}
